$(document).ready(function(){
    // Initialize BxSlider
    $('.images').bxSlider({
      auto: true,          // Enable automatic sliding
      pause: 5000,         // Set the time between slides in milliseconds (e.g., 5000ms = 5 seconds)
      mode: 'horizontal',        // Set the transition mode (you can change it to 'horizontal' for horizontal sliding)
      captions: false       // Display image captions (you can set it to true if you have captions)
    });
  });

  $(document).ready(function(){
    // Initialize BxSlider
    $('.images2').bxSlider({
      auto: true,          // Enable automatic sliding
      pause: 5000,         // Set the time between slides in milliseconds (e.g., 5000ms = 5 seconds)
      mode: 'horizontal',        // Set the transition mode (you can change it to 'horizontal' for horizontal sliding)
      captions: false       // Display image captions (you can set it to true if you have captions)
    });
  });

  